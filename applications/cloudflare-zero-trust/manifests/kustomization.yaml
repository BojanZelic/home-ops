apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

configurations:
  - nameReference.yaml

namespace: zero-trust-system

resources:
  - https://github.com/Qalisa/cloudflare-zero-trust-operator//config/default?ref=v4
  - secret.yaml
