apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: frigate
spec:
  storageClassName: local-storage
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 450Gi
  volumeName: "frigate"