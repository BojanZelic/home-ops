repositories:
- name: gitlab
  url: https://charts.gitlab.io

releases:
- name: runner
  namespace: gitlab-managed-apps
  chart: gitlab/gitlab-agent
  version: 0.6.1
  installed: false
  values:
    - values.yml.gotmpl
    #- values.yaml.gotmpl
