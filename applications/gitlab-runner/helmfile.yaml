repositories:
- name: gitlab
  url: https://charts.gitlab.io

releases:
- name: runner
  namespace: gitlab-agent
  chart: gitlab/gitlab-agent
  #version: 0.40.0
  installed: true
  values:
    - values.yml.gotmpl
    #- values.yaml.gotmpl
